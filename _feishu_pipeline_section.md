## 从整个链路理解系统：原理与可改进方向

这套系统的核心不是“把 Excel 直接交给大模型生成报告”，而是把原始数据逐步加工成可追溯、可验证、可渲染的分析产物。每一步都有自己的输入、输出和责任边界：前一步负责把数据准备好，后一步只能使用已经确认的结果。

### 1. 原始输入：先确定数据事实边界

原始输入通常是 Excel 或 CSV。系统首先读取源文件、工作表和原始列，检查文件是否存在、必需列是否缺失、数值是否可转换、维度是否为空。

这一步的原理是“先确认事实，再进行分析”。如果源数据没有客户 ID，就不能把订单数说成去重用户数；如果没有市场份额，就不能生成竞品份额结论。

后期可以改进的地方：

- 增加更多输入格式，例如数据库查询结果、API 返回结果或标准化 CSV。
- 增加输入数据质量报告，明确记录缺失列、空值、异常值和时间范围。
- 为不同来源建立版本号和来源标识，方便追溯“这条结论来自哪一份数据”。
- 保持当前边界：数据不足时标记“数据不足”或 `NOT_EXECUTABLE`，不通过默认值掩盖问题。

### 2. 字段语义映射：把源列转换成业务字段

字段映射把 Excel 的实际列名转换成系统统一使用的标准字段。例如源表中的“获客总成本”映射为 `cost`，“三级渠道”映射为 `channel`，“首单营收”映射为 `first_order_revenue`。

这样做的原理是解耦“数据来源的叫法”和“指标逻辑的叫法”。源文件列名变化时，优先修改映射配置，而不是修改所有指标和规则代码。

后期可以改进的地方：

- 在 `FIELD_MAP` 中继续增加字段，例如退款金额、毛利、客户 ID、订单 ID、投放平台、地区等。
- 为每个字段补充类型、单位、是否允许为空、业务含义和来源列名。
- 支持同一个标准字段对应多个候选源列，并记录最终采用了哪一列。
- 对字段映射做可视化预览，让使用者在计算前确认“源列 → 标准字段”的对应关系。
- 当加入客户 ID、订单 ID 等更细粒度字段后，才可以扩展去重用户、复购和客户生命周期分析。

### 3. 聚合：确定分析粒度并汇总原始记录

聚合把明细记录按照业务维度分组。目前主要采用 `month + channel` 或 `month + category`。成本、订单数、营收等加总字段先求和；转化率、ROI、CAC 等比率不能直接平均，而应先汇总分子和分母，再重新计算。

聚合的原理是让后续规则在稳定的分析粒度上工作，同时避免“平均值的平均值”造成统计偏差。

后期可以改进的地方：

- 调整聚合粒度，例如从月度改为日、周、季度，或增加平台、地区、账户、投放计划等维度。
- 支持多层级聚合，例如先按 `month + channel`，再按全渠道汇总。
- 根据字段类型维护聚合策略：金额和计数求和，率类重新计算，去重指标按 ID 去重。
- 增加粒度配置和聚合结果审计，明确每个结果由多少条原始记录汇总而来。
- 跨月规则仍应只使用连续完整月份，不能把不完整月份直接当成完整月份参与趋势判断。

### 4. 指标计算：把聚合数据转成可解释指标

指标层根据标准字段计算 CAC、ROI、重复线索率等业务指标，并使用安全除法处理分母为零、缺失或不可用的情况。指标结果应同时保留计算所需的分子、分母和时间范围。

指标层的原理是把业务口径固化为机器可执行的计算，避免后面的 Prompt 或人工临时解释公式。

后期可以改进的地方：

- 增加毛利 ROI、退款后营收、LTV、回本周期等指标，但必须先确认业务定义。
- 给每个指标增加公式、单位、口径版本和证据来源。
- 支持按配置启用或停用指标，避免把尚未确认的指标混入正式结果。
- 对指标增加分母覆盖率、异常值和极端值检查。
- 区分展示指标和规则指标，例如 `cac` 用于展示，`cac_rate` 用于 R36/R37，不能混用。

### 5. Rule Engine：把业务判断变成可审计规则

Rule Engine 读取指标表，逐条输出规则评价。每条结果都应包含规则 ID、判断结果 `hit`、维度、观察周期、指标快照、阈值和证据。命中规则才可以支持风险或机会判断；未命中只能说明该规则没有触发，不能被扩展成全面健康结论。

规则层的原理是把“事实”和“判断”分开：指标是事实，规则是明确的判断条件，报告只消费已经执行过的规则结果。

后期可以改进的地方：

- 增加新的规则，例如渠道波动、品类集中度、退款异常、毛利下降、预算消耗速度等。
- 为规则增加优先级、严重程度、适用范围和规则版本。
- 将部分阈值配置化，例如不同渠道或业务线使用不同的 ROI 红线。
- 增加规则单元测试、边界值测试和历史回放测试。
- 对暂时没有数据支持的规则保留 `NOT_EXECUTABLE`，不要用相似字段替代真实业务字段。

### 6. Prompt：把事实包组装成受约束的分析任务

Prompt 不负责计算指标，也不负责创造事实。它的作用是把 Rule Result、事实证据、报告结构和表达要求组装成模型可执行的任务，并明确“只能依据输入资料”“命中规则才可做判断”“资料不足时写数据不足”。

Prompt 层的原理是控制模型的解释空间，让模型做归纳、排序和表达，而不是重新发明数据口径。

后期可以改进的地方：

- 按报告类型拆分 Prompt，例如渠道诊断、品类分析、效率分析和管理层摘要。
- 增加固定输出字段，如结论、证据、影响、建议、数据缺口和置信边界。
- 增加少量经过审核的 Few-shot 示例，帮助模型稳定输出格式和语气。
- 将规则结果、事实包、数据缺口和格式要求分开传入，避免内容和格式互相污染。
- 增加 Prompt 版本号和回归样例，比较不同版本是否出现新增无证据判断。

### 7. 报告结构与 Semantic Blocks：把内容和格式分离

模型输出的内容先转换成结构化语义块，例如标题、摘要、指标卡、表格、结论、建议和数据缺口。Semantic Blocks 保存内容顺序和语义角色；Renderer 决定这些内容最终如何排版。

这一层的原理是“内容与格式分离”。同一份事实内容可以渲染成 Markdown、飞书文档、Word 或网页，而不必重新生成事实。

后期可以改进的地方：

- 增加更多语义块类型，如趋势图、规则证据块、风险等级块和行动项块。
- 对语义块增加 Schema 校验，确保必填字段、顺序和类型正确。
- 支持不同受众的版式，例如管理层摘要版、运营明细版和审计版。
- 允许只改 Renderer 和资源映射，不改上游事实文本。
- 对每个语义块保留 `evidence_ref` 或 `conclusion_id`，实现结论到规则证据的反向追溯。

### 8. DOCX、飞书文档与画板：把结构化内容渲染成交付物

Renderer 将 Semantic Blocks 转换为最终文档。正文、标题、表格、图片和画板属于不同类型的文档资源，应按各自的渲染规则处理。流程、架构和指标关系适合使用原生画板；详细事实和规则明细仍应保留在正文或表格中。

这一层的原理是让交付格式服务于阅读目的：文字承载事实，表格承载明细，画板承载关系和流程。

后期可以改进的地方：

- 增加 Markdown、DOCX、飞书文档、网页等多种 Renderer。
- 把字体、标题层级、表格样式、页边距和画板风格统一配置化。
- 根据内容长度自动分页，并对表格跨页、标题孤行和图片尺寸进行处理。
- 让画板从同一份流程或结构数据生成，避免正文和画板出现口径不一致。
- 保留原生可编辑画板，而不是只插入一张静态截图。

### 9. QA 与反馈闭环：验证事实、结构、视觉和可用性

最终 QA 不能只看脚本是否运行成功。至少要分别验证：事实是否来自输入包，规则结果是否完整保留，语义块是否符合 Schema，DOCX/飞书文档是否成功保存，画板是否可打开，以及页面视觉是否存在溢出、重叠或不可读问题。

后期可以改进的地方：

- 增加事实保留率、规则命中一致性、证据引用完整性和格式通过率指标。
- 对同一输入固定生成回归快照，比较代码、规则或 Prompt 修改前后的变化。
- 增加人工验收清单，覆盖真实 API、审批状态、文档回读和浏览器展示。
- 将 QA 结果和版本信息一起保存，形成可追溯的交付记录。

### 10. 全链路的扩展原则

后期扩展时，建议遵循以下顺序：先增加或确认输入字段，再更新字段映射；然后明确新的聚合粒度和指标公式；再增加可执行规则；之后更新事实包和 Prompt；最后修改 Semantic Blocks、Renderer 或画板。不要跳过中间层，直接在 Prompt 中“补出”上游没有的数据。

可以把整条链路概括为：

`Raw Input → Field Mapping → Aggregation → Metrics → Rule Result → Prompt Assembly → Semantic Blocks → Renderer → DOCX/飞书/画板 → QA`

其中每一层都应该能够单独检查、单独替换，并保留输入输出证据。这样新增字段、修改聚合粒度、扩展计算规则、调整 Prompt 或更换最终格式时，才不会破坏整条链路的可解释性和可追溯性。
